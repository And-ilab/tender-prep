# Интеграция с parserit (Windmill)

**parserit** — пространство сценариев в Windmill, где выполняются парсинг и нормализация документов закупки. Сервис **«Лена»** не дублирует этот рантайм: он оркестрирует запросы и потребляет результаты по согласованному контракту.

## Идентификация ресурсов в Windmill

В UI Windmill скрипты и флоу имеют пути вида:

- **Скрипт**: `f/parserit/<имя>` или `u/<user>/<имя>` — в API и MCP часто указывают путь **без** ведущего слэша, как в интерфейсе (например, `u/user/my_script`).
- **Флоу**: папка под `f/`, в описаниях интеграций встречается форма вроде `parserit/it_parser_flow` для флоу, расположенного в UI как `f/parserit/it_parser_flow`.

Конкретные имена скриптов и флоу **не зашиты** в этом репозитории: их фиксирует команда в конфигурации сервиса Лена или в переменных окружения. Ниже — **шаблон** договорённостей; замените плейсхолдеры на актуальные пути из вашего Windmill.

| Плейсхолдер | Пример назначения |
|-------------|-------------------|
| `FLOW_PARSE_TENDER` | Полный разбор комплекта документов в единую структуру |
| `SCRIPT_EXTRACT_LOT` | Быстрое извлечение лота/предмета из извещения |
| `SCRIPT_OCR_TABLE` | Таблицы из сканов (если используется отдельным шагом) |

## Входные данные (типовой контракт)

На стороне parserit ожидается JSON с полями, согласованными с флоу. Типовой набор (адаптируйте под реальный редактор флоу в Windmill):

```json
{
  "tender_id": "внутренний-id-или-реестровый",
  "document_urls": ["https://...", "s3://..."],
  "locale": "ru",
  "options": {
    "extract_requirements": true,
    "extract_deadlines": true
  }
}
```

**Важно:** не передавайте в публичные эндпоинты персональные данные и коммерческую тайну сверх того, что уже разрешено политикой компании и договором с оператором Windmill.

## Полный архив на Drive без ручной выборки

Если нужно **парсить все файлы подряд** в большой папке (архив за год и т.д.), в `tender-prep` нет встроенного движка PDF/OCR — очередь заданий формируется **снаружи** (parserit / свой воркер).

1. Сформируйте **JSONL** — по одной строке JSON на каждый файл на Drive (id, mime, путь, ссылка):

   ```bash
   node src/cli.js drive corpus-jsonl "https://drive.google.com/drive/folders/ARCHIVE_ID" 40 200000 > corpus-all.jsonl
   ```

2. В Windmill (или другом рантайме) **прочитайте файл построчно** и для каждой строки вызовите ваш флоу парсинга: в теле задания используйте поле **`documentUrl`** (или массив `document_urls` из одного элемента) плюс **`path`** как якорь источника в ответе.

3. Либо после **`drive corpus-pull`** обходите **`C:\data\…`** локально тем же parserit, если флоу умеет читать путь на диске вместо URL.

Идемпотентность и кэш по `driveFileId` — на стороне parserit (см. раздел про ошибки ниже).

## Выходные данные

Ожидается структурированный результат (JSON), из которого Лена строит:

- список **требований** с якорями на источник (файл, фрагмент);
- **сроки** и ключевые даты;
- при наличии — **критерии оценки** и штрафные санкции.

Любое поле, которое парсер не смог извлечь надёжно, должно помечаться в ответе (`confidence`, `needs_review`, аналог) — Лена обязана пробрасывать эту пометку в UI или в текст для оператора.

### Матрица требований: только документы заказчика (без додумываний)

Строки **`requirements`**, на основе которых Лена строит матрицу соответствия (`node src/cli.js matrix …`), **не должны** опираться на «типовой пакет заявки», общие знания модели, внешний RAG или практику других закупок. **Источник истины** — только текст (включая OCR) файлов, переданных в это задание парсинга.

**Запрещено:**

- добавлять в `requirements` позиции вроде «справка банка», «выписка из торгового реестра», «референс-лист» и т.п., если в переданных документах **нет** явного требования или перечня, из которого это следует;
- заполнять матрицу «наоснове опыта закупок в РБ» или шаблонов участника;
- подменять отсутствие данных выдуманными формулировками вместо пустого/короткого списка.

**Обязательно:**

- каждый объект в `requirements` имеет проверяемую привязку **`source`** (`file` из комплекта задания; по возможности `fragment` — короткая **дословная** цитата или однозначный указатель раздела из этого же текста; при наличии — `page`);
- поле **`text`** формулирует одно требование и не смешивает несвязанные пункты; если формулировку нельзя обосновать цитатой или явным пунктом из текста — **не включай** строку (либо пометь `needs_review: true` и низкий `confidence`, без добавления «типовых» документов);
- с **`deadlines`** и **`criteria`** — те же правила: только из текста входных документов, с тем же уровнем привязки к `source`.

Пример целевой формы ответа см. в репозитории: `examples/parser-result.sample.json`.

#### Копируемый фрагмент для system-промпта LLM (шаг извлечения требований)

Вставьте в Windmill как системные инструкции к модели, которая формирует JSON результата парсинга (или только блок `requirements`). Температура рекомендуется низкая (0–0.2).

```text
Ты извлекаешь структурированные данные из документов закупки для последующей матрицы соответствия. Отвечай только валидным JSON по схеме задания, без Markdown и без комментариев вне JSON.

Источник: ТОЛЬКО текст документов, переданных в этом запросе (комплект заказчика). Не используй внешние знания, типовые списки документов заявки, практику других тендеров и архивные примеры.

Требования к полю requirements:
- Каждая строка — одно требование или один документ/условие из явного перечня заказчика, действительно присутствующее во входном тексте.
- Поле text — формулировка, выведенная из текста документа (сжатый пересказ одного места допускается; не объединяй несвязанные пункты).
- Поле source.file — имя/идентификатор файла из входного комплекта. Поле source.fragment — короткая дословная цитата ИЛИ однозначная ссылка на раздел/пункт из этого файла, по которой проверяющий находит то же требование. Если такую привязку указать нельзя — не добавляй строку.
- Не дополняй список «стандартными для РБ» справками, выписками, референсами и т.д., если их нет во входных документах.
- Если явного перечня требований во входе нет — верни пустой массив requirements или только те пункты, что однозначно извлекаются; не заполняй матрицу выдуманными строками.
- При сомнении: omit (исключи пункт) или выставь needs_review: true и низкий confidence; не выдавай галлюцинации за факты.

deadlines и criteria — те же правила опоры только на входные документы и обязательная проверяемая source, где это предусмотрено схемой.
```

## Вызов из автоматизации

1. Поставить в очередь job Windmill на нужный **script path** или **flow path** с аргументами, совпадающими с полями флоу.
2. При длительном разборе использовать режим ожидания результата или polling по `job_id` — в зависимости от клиента API / MCP.
3. Сохранить сырой ответ parserit (для аудита) отдельно от нормализованного представления в Лене.

## Ошибки и деградация

- **Таймаут** — повторить с идемпотентным `tender_id`, не дублируя загрузку файлов на стороне хранилища, если уже есть кэш.
- **4xx/валидация** — проверить соответствие ключей JSON схеме флоу в Windmill.
- **Частичный результат** — не считать матрицу соответствия полной; явно запросить ручной ввод для пробелов.

## Связанные документы

- [PRODUCT_CONTEXT.md](PRODUCT_CONTEXT.md) — зачем Лена и какие границы у автоматизации.
- [WINDOWS_GIT.md](WINDOWS_GIT.md) — если секреты и URL Windmill задаются через локальные конфиги в репозитории (не коммитьте токены).

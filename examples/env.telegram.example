# Не коммитить. Скопируйте в .env или задайте в shell.

# Токен от @BotFather (обязательно для любого бота)
TELEGRAM_BOT_TOKEN=123456789:ABC...

# Telegram · IceTrade: по умолчанию после ссылки только импорт в inputs (файлы + JSON), без парсинга и без LLM.
# Полный конвейер (анализ, extract, карточка) — явно: LENA_TELEGRAM_ICETRADE_IMPORT_ONLY=0
# LENA_TELEGRAM_ICETRADE_IMPORT_ONLY=1
# Индикатор внизу чата (sendChatAction): значения **на английском**, как в Bot API — typing, upload_document, find_location, …
# Клиент Telegram может показывать локализованную подпись («печатает…», «отправляет документ»…).
# LENA_TELEGRAM_CHAT_ACTION_IMPORT=upload_document   # импорт IceTrade → Drive (по умолчанию)
# LENA_TELEGRAM_CHAT_ACTION_PARSE=typing            # парсинг + Analysis (по умолчанию)
# LENA_TELEGRAM_CHAT_ACTION_LLM=typing              # КП и прочий LLM (по умолчанию)
# LENA_TELEGRAM_KP_SKIP_MANAGER_PRICE_GATE=1        # без запроса цены перед кнопками ГС Ритейл / Финсельват (см. бот)
# Устарело (читается, если не задан LENA_TELEGRAM_CHAT_ACTION_IMPORT): LENA_TELEGRAM_BOOTSTRAP_CHAT_ACTION=typing
# LENA_TELEGRAM_BOOTSTRAP_CHAT_ACTION=typing

# Бот «Лена» (src/telegram/lena-bot.mjs): корень Drive — id или URL папки
LENA_DRIVE_ROOT=https://drive.google.com/drive/folders/...
# Тот же LENA_DRIVE_ROOT использует scripts/icetrade-headless-smoke.mjs: после скачивания заливает playwright-downloads → _lena/tenders/…/inputs/

# JSON ключ сервисного аккаунта (путь к файлу) — если НЕ используете OAuth ниже
# GOOGLE_DRIVE_CREDENTIALS=C:\Users\bkp2.LAPTOP-714KMOB9\Downloads\tender-prep-drive-637351043f8c.json

# --- Личный Google (OAuth): вместо SA, без Shared drive (см. docs/GOOGLE_DRIVE_OAUTH.md) ---
# GOOGLE_DRIVE_OAUTH_CLIENT=C:\secrets\google-oauth-client.json
# GOOGLE_DRIVE_OAUTH_TOKEN=C:\secrets\lena-drive-user-token.json
# Один раз: node src/cli.js drive oauth-login

# Опционально: только эти чаты (id через запятую). Узнать id группы: @userinfobot или лог бота.
# TELEGRAM_ALLOWED_CHAT_IDS=-1001234567890

# Опционально: год для /bundle (по умолчанию LENA_DEFAULT_TENDER_YEAR или текущий год)
# LENA_DEFAULT_TENDER_YEAR=2026

# Опционально: дополнительные папки контекста на Drive (см. docs/GOOGLE_DRIVE.md)
# LENA_EXTRA_CONTEXT_FOLDERS=https://drive.google.com/drive/folders/...,https://...

# --- LLM (/ask, /tenderask): OpenAI-compatible Chat Completions ---
# OPENAI_API_KEY=sk-...
# или отдельный ключ только для Лены:
# LENA_OPENAI_API_KEY=sk-...
# LENA_OPENAI_BASE_URL=https://api.openai.com/v1
# LENA_OPENAI_MODEL=gpt-4o-mini
# Пример чата через DeepSeek (OpenAI-compatible):
# LENA_OPENAI_BASE_URL=https://api.deepseek.com/v1
# LENA_OPENAI_MODEL=deepseek-chat
# LENA_TELEGRAM_GROUP_ASK_REQUIRE_REPLY=1   # в группе: /ask только как «Ответить» на сообщение (см. docs/TELEGRAM.md)

# --- IceTrade bootstrap (ссылка в чате или node src/cli.js tenders icetrade-bootstrap …) ---
# Рекомендуемый порядок, если вложения не качаются или «HTML вместо PDF» (часто публичные карточки в браузере ок):
#   1) LENA_ICETRADE_PLAYWRIGHT=1 и: npm install && npx playwright install chromium
#      Служба Windows (NSSM): общий каталог браузера — LENA_PLAYWRIGHT_BROWSERS_PATH=C:\ProgramData\ms-playwright
#      (install-windows.ps1 ставит Chromium туда автоматически)
#   2) Опционально, чтобы скрипт был ближе к браузеру: LENA_ICETRADE_COOKIE из DevTools **или**
#      LENA_ICETRADE_PLAYWRIGHT_STORAGE (JSON после playwright-auth):
#      npm run icetrade:playwright-auth -- C:\secrets\icetrade-storage.json
#      (по умолчанию открывается https://icetrade.by/tenders/all/view/1336510 )
#      или с явным URL: … -- C:\secrets\icetrade-storage.json https://icetrade.by/tenders/all/view/1336510
#      (или id … 1336510 ; иначе LENA_ICETRADE_PLAYWRIGHT_AUTH_START_URL в .env)
#      Не задавайте STORAGE на несуществующий путь.
#   3) При необходимости: LENA_ICETRADE_PLAYWRIGHT_DOWNLOAD_PRIME_MS=3500,
#      LENA_ICETRADE_FETCH_TIMEOUT_MS=40000, LENA_ICETRADE_PLAYWRIGHT_SETTLE_MS=8000
#   4) Перезапуск бота после правок .env
#
# LENA_ICETRADE_BOOT_MAX_FILES=30
# LENA_ICETRADE_FETCH_TIMEOUT_MS=25000
# LENA_ICETRADE_PLAYWRIGHT=1
# LENA_PLAYWRIGHT_BROWSERS_PATH=C:\ProgramData\ms-playwright
# При 1 вложения качаются через Chromium (даже если HTML карточки пришёл через PowerShell). 0 в следующей строке отключает только этот шаг:
# LENA_ICETRADE_PLAYWRIGHT_FILE_DOWNLOAD=0
# Пауза после открытия карточки в том же Chromium перед GET вложений (мс):
# LENA_ICETRADE_PLAYWRIGHT_DOWNLOAD_PRIME_MS=2000
# Если «Аукционные документы» на карточке всегда раскрыты — не кликать по вкладкам (быстрее)
# LENA_ICETRADE_PLAYWRIGHT_SKIP_DOC_UI_CLICKS=1
# LENA_ICETRADE_PLAYWRIGHT_STORAGE=C:\secrets\icetrade-storage.json
# Опционально: карточка для icetrade:playwright-auth, если не передаёте URL вторым аргументом npm:
# LENA_ICETRADE_PLAYWRIGHT_AUTH_START_URL=https://icetrade.by/tenders/all/view/1336510
# LENA_ICETRADE_PLAYWRIGHT_SETTLE_MS=6000
# Постоянная папка для acceptDownloads: иначе Playwright кладёт файлы во временный каталог и при закрытии
# окна записи в Chrome пропадают («Показать в папке» не работает). По умолчанию в коде: <cwd>/playwright-downloads
# LENA_ICETRADE_PLAYWRIGHT_DOWNLOADS_DIR=C:\Users\YourName\Downloads\tender-prep
# LENA_ICETRADE_PLAYWRIGHT_DOWNLOADS_DIR=playwright-downloads
# Не указывайте системный %TEMP%/«Местные временные файлы» — первая за день очистка корня удалит чужие каталоги,
# в т.ч. lena-ice-* импорта IceTrade → ENOENT при записи заметки bootstrap.
# Файлы кладутся в <корень>/<viewId>/; первый запуск Playwright за локальный день очищает корень (кроме маркера .lena-playwright-dl-last-clean).
# LENA_ICETRADE_PLAYWRIGHT_DOWNLOADS_SKIP_DAILY_CLEANUP=1
# LENA_ICETRADE_PLAYWRIGHT_MAX_RESPONSE_BYTES=4000000
# Стабильность: пауза между файлами (мс), повтор при обрыве
# LENA_ICETRADE_DOWNLOAD_GAP_MS=350
# LENA_ICETRADE_DOWNLOAD_RETRIES=3
# LENA_ICETRADE_DOWNLOAD_RETRY_GAP_MS=700
# LENA_ICETRADE_COOKIE=name=value; name2=value2
# Малые PDF на Drive (< порога) считаются «заглушками» и заменяются при следующем bootstrap (байт, по умолчанию 98304 ≈ 96 KiB):
# LENA_ICETRADE_REPLACE_PDF_IF_BYTES_BELOW=98304
# Python fetch_card.py: always | auto (только если Node не дал кандидатов) | выкл.
# LENA_ICETRADE_PYTHON_FETCH=auto
# LENA_ICETRADE_PYTHON=python

# Анализ комплекта после bootstrap (Telegram / заметка icetrade-analysis-*.md): только фрагменты из inputs
# (файлы + icetrade-import-snapshot.json с карточки). Пост-фильтр убирает пункты без дословной цитаты из текста.
# LENA_ICETRADE_ANALYZE_STRICT_GROUNDING=0   # выкл. жёсткую привязку (по умолчанию вкл.)
# LENA_ICETRADE_ANALYZE_USE_RAG=1             # подмешать RAG в промпт (пункты матрицы всё равно только из inputs)
# Явный @username менеджера в заголовке «Требуется помощь менеджера» в матрице Analysis (Telegram / Drive / Preparation).
# Если не задано — username в текст не подставляется (удобно на этапе тестирования).
# LENA_MANAGER_TELEGRAM_MENTION=@username

# В Telegram парсинг **inputs** после импорта — кнопка «Анализ документов» или /tenderextract (авто после ссылки не запускается).
# В полном конвейере при LENA_TELEGRAM_ICETRADE_IMPORT_ONLY=0 — отдельно карточка (LLM по extracted):
# LENA_TELEGRAM_CARD_AFTER_BOOTSTRAP=1

# LENA_LLM_SYSTEM_PROMPT=…  # см. docs/LENA_RULES.md — раздел «Краткий системный блок»

# --- Парсинг PDF (качество: Python PyMuPDF + системный Tesseract; Node — запасной путь) ---
# pip install -r requirements-ocr.txt — в PATH системный tesseract с traineddata rus
# LENA_PYTHON=C:\Python311\python.exe
# LENA_PDF_OCR_PYTHON=0                 # отключить Python (только Node tesseract.js)
# LENA_OCR_TESSERACT=tesseract          # или полный путь к tesseract.exe
# LENA_OCR_DISABLE=1
# LENA_OCR_PDF_MIN_TEXT_CHARS=48
# LENA_OCR_PDF_MAX_PAGES=30
# LENA_OCR_PDF_SCALE=3.25               # только fallback на Node (pdf.js), по умолчанию 3.25, максимум 4
# LENA_OCR_PDF_IGNORE_TEXT_LAYER=1      # всегда только растр+OCR (игнор встроенного слоя в PyMuPDF)
# LENA_OCR_PDF_SKIP_CORRUPT_HEURISTIC=1 # отключить эвристики «битый» слой (Node и Python)
# LENA_OCR_PDF_MOJIBAKE_CJK_RATIO=0.018 # всплеск CJK в русском PDF → принудительно OCR/повторное извлечение
# LENA_OCR_PDF_FRAG_SHORT_LINE_RATIO=0.33 # много строк ≤3 символов → «лесенка» после pdf-parse, идём в пайплайн
# LENA_OCR_TESSERACT_PSM=3              # в Python, Tesseract PSM (3=авто; попробуйте 4 или 6)
# LENA_OCR_TESSERACT_LANG=rus+eng       # языки Tesseract (Python CLI и Node fallback); нужны traineddata rus и eng
# LENA_OCR_PDF_TAB_GAP_PT=10            # порог зазора (pt) → таб между колонками в dict-режиме
# Язык OCR везде rus.
# --- Локальный RAG (node src/cli.js rag index|query|ask) ---
# Индекс использует POST …/embeddings. У DeepSeek обычно нет этого endpoint — задайте отдельный хост эмбеддингов:
# LENA_EMBEDDING_BASE_URL=https://…/v1
# LENA_EMBEDDING_API_KEY=sk-…   # если не тот же, что LENA_OPENAI_API_KEY
# LENA_EMBEDDING_MODEL=text-embedding-3-small
# LENA_EMBEDDING_DIMENSIONS=1536
# Локальные эмбеддинги на CPU (см. scripts/local_openai_embeddings/README.md):
# set LENA_EMBEDDING_BASE_URL=http://127.0.0.1:8765/v1
# set LENA_EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
# Опционально: архив тендеров (RAG) в Telegram — /archivesearch, /archiveask
# LENA_RAG_INDEX_DIR=C:\data\rag-index-2025-drive
# LENA_RAG_TOP_K=8

# --- КП: Google Doc по шаблону (копия в drafts + подстановка текста LLM) ---
# В GCP включите API **Google Docs** для того же проекта, что и Drive. После смены OAuth-scope: node src/cli.js drive oauth-login
# Папка шаблонов: подпапки **gs-retail** и **finselvat**, в каждой — Google Doc (имя с «КП» / «шаблон» предпочитается).
# LENA_CP_DOC_TEMPLATE_FOLDER_ID=https://drive.google.com/drive/folders/1f9jKjbExGpydO7gBFk38jDsWSPubxbh9
# Или явный id Doc на компанию:
# LENA_CP_DOC_TEMPLATE_ID_GS_RETAIL=https://docs.google.com/document/d/…/edit
# LENA_CP_DOC_TEMPLATE_ID_FINSELVAT=https://docs.google.com/document/d/…/edit
# В шаблоне абзац с текстом {{LENA_KP_BODY}} — на это место подставляется черновик (plain). Без маркера текст дописывается в конец.
# LENA_CP_SAVE_MARKDOWN=0   # не сохранять дубль .md (по умолчанию сохраняется)
# Порог НМЦ/цены лота (BYN): строки с ключевыми словами и сумма ниже порога → явное согласование цены с менеджером (по умолчанию 10 000).
# LENA_CP_COORDINATION_PRICE_BELOW_BYN=10000

# Реквизиты организации-участника для промпта КП (txt/md/json, Google Doc/Sheet в папке):
# LENA_ORG_REQUISITES_FOLDER_ID=https://drive.google.com/drive/folders/1LDtfNOXmjqMvCMUz-OZrE__xFDezgZMo
